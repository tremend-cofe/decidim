<%= cell("decidim/announcement", t(".announcement"), callout_class: "warning" ) %>

<div class="card">
  <div class="card-divider">
    <h2 class="card-title">
      <%= t ".title" %>
      <% if allowed_to?(:create, :stopwords) %>
        <%= link_to t("actions.new", scope: "decidim.admin", name: t("stopword.name", scope: "decidim.models").downcase), [:new, :stopword], class: "button tiny button--title new" %>
      <% end %>
    </h2>
  </div>
  <div class="card-section">
    <% if collection.any? %>
      <div class="table-scroll">
        <table class="table-list stopwords">
          <thead>
          <tr>
            <th><%= t(".word") %></th>
            <th><%= t("stopword.fields.created_at", scope: "decidim.models") %></th>
            <th></th>
          </tr>
          </thead>
          <tbody>
          <% collection.each do |word| %>
            <tr data-keyword-id="<%= word.id %>">
              <td><%= link_to_if allowed_to?(:update, :stopwords, word:) , word.word, edit_stopword_path(word) %></td>
              <td><%= l word.created_at, format: :long %></td>
              <td class="table-list__actions">
                <% if allowed_to?(:update, :stopwords, word: word) %>
                  <%= icon_link_to "pencil", edit_stopword_path(word), t("actions.edit", scope: "decidim.admin"), class: "edit" %>
                <% end %>

                <% if allowed_to?(:destroy, :stopwords, word: word) %>
                  <%= icon_link_to "circle-x", stopword_path(word), t("actions.destroy", scope: "decidim.admin"), method: :delete, data: { confirm: t(".confirm_delete") }, class: "action-icon--remove" %>
                <% end %>
              </td>
            </tr>
          <% end %>

          </tbody>
        </table>
        <%= paginate collection, theme: "decidim" %>
      </div>
    <% else %>
      <%= t(".empty") %>
    <% end %>
  </div>
</div>
